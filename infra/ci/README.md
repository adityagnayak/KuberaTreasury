# CI Security Gates (Policy Placeholder)

Required checks for Phase 2 onwards:
1. Python dependency scan: `pip-audit`
2. Frontend dependency scan: `npm audit`
3. Secret scanning (e.g., gitleaks)
4. Migration lint and policy checks
5. SBOM generation and artifact attestation (recommended)
